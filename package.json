{
  "name": "backbone-chat-app",
  "version": "1.0.0",
  "description": "simple chat application simular to programmatic chat app in ios",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "browserify public/js/index.js -o public/js/bundle.js",
    "sass": "node-sass --output-style expanded public/css/styles.scss public/css/styles.css",
    "build": "npm run bundle && npm run sass"
  },
  "author": "Andy Coles",
  "license": "ISC",
  "dependencies": {
    "backbone": "^1.2.3",
    "jquery": "^2.2.0",
    "underscore": "^1.8.3"
  }
}
